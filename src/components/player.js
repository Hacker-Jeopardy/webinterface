import React from 'react';

const Player = (props) => {
  return (
    <div>Player</div>
  );
};

Player.displayName = 'Player';
Player.propTypes = {};
Player.defaultProps = {};

export default Player;